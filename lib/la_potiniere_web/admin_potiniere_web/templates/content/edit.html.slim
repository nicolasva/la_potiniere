h2 Edition du contenu

= render "form.html", changeset: @changeset, action: Routes.admin_menu_content_path(@conn, :update, @menu_id, @content)

= link "Retour", to: Routes.admin_menu_content_path(@conn, :index, @menu_id)
